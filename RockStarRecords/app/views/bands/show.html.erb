<h1><%= @band.name %></h1>

<h3>Albums:</h3>

<ul>
  <% @band.albums.each do |album| %>
    <li>
      <%= link_to "#{album.title}", album_url(album) %>
    </li><br/>
  <% end %>
</ul>

<h3> <%= link_to 'Back to catalogue', bands_url %></h3>
